# distributed-clock-simulator
A simple distributed clock simulator to illustrate the concept of logical clocks. TCP sockets are used to simulate the network between clients and servers.

## Getting Started
### Clone the repository
git clone https://github.com/bdnguyen/distributed-clock-simulator.git

### Navigate to the project directory
cd distributed-clock-simulator

### Build the project
cargo build

### Run the project
cargo run
